import React from 'react';
import Styles from '../Styles/Mount.css'

const Mounting = () => {
  return (
    <>
    <div>
    <h2>Mounting</h2>
    <p>React supports three mounting lifecycle methods for component classes: componentWillMount(), render(), and componentDidMount(). componentWillMount() will be called first followed by the render() method and finally the componentDidMount() method.</p>
 </div>
 </>
  )
}

export default Mounting